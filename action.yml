name: 'cancel-workflows-action'
description: 'Action to cancel workflows'
author: 'cactusoft-ca'
inputs:
  github_token:  # github token
    description: 'Token to authentify to github'
    required: true
  workflow_run_id:  # id of input
    description: 'The current id of workflow run'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'